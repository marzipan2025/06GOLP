# 06GOLP

Good Old Launchpad for macOS.

`06GOLP` is a lightweight Launchpad-style app launcher. It stays in the background and opens with `Option + Space`.

## Build

```sh
./scripts/build_app.sh
```

The app bundle is created at `Build/06GOLP.app`.
